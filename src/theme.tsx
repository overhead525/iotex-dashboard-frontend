import { createTheme } from "@material-ui/core";

import "./index.css";

export const dashboardTheme = createTheme({
  typography: {
    fontFamily: ["'IBM Plex Sans', sans-serif", "'DM Mono', monospace"],
  },
});
